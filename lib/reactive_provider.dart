import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

class ReactiveProvider extends Provider {
  ReactiveProvider({Key? key, required super.create}) : super(key: key);

  
}
